import React from 'react';
import styled from 'styled-components';

const Wrap = styled.div`
    font-weight: 300;
    font-size: 1.2rem;
    font-family: "pretendard";
    font-weight: 300;
`;

const InputPlaceholder = () => (
    <Wrap>메모를 입력해주세요</Wrap>
);

export default InputPlaceholder;